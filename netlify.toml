[build]
  publish = "public"
  functions = "netlify/functions"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "18"

# API redirects for serverless functions
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

# Admin page redirect (backward compatibility)
[[redirects]]
  from = "/admin/manage-prompts.html"
  to = "/admin-controlpanel.html"
  status = 301

[[redirects]]
  from = "/admin/manage-promos.html"
  to = "/admin-controlpanel.html"
  status = 301

# SECURITY: Block access to config files containing API keys
[[redirects]]
  from = "/config/*"
  to = "/404.html"
  status = 404

[[redirects]]
  from = "/*.env"
  to = "/404.html"
  status = 404

[[redirects]]
  from = "/env-*"
  to = "/404.html"
  status = 404

# SPA fallback for client-side routing (removed invalid conditions)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Security headers for all pages
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "camera=(), microphone=(), geolocation=()"
    Content-Security-Policy = "default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval' https://unpkg.com https://cdn.jsdelivr.net https://js.stripe.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; img-src 'self' data: https:; frame-src 'self' https://js.stripe.com https://*.stripe.com; connect-src 'self' https://*.supabase.co https://api.stripe.com https://*.stripe.com;"

# Cache headers for static assets
[[headers]]
  for = "/css/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/js/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/templates/*"
  [headers.values]
    Cache-Control = "public, max-age=86400"

# Function configuration
[functions]
  external_node_modules = ["@supabase/supabase-js", "openai", "stripe"]
  node_bundler = "esbuild"

# Individual function timeouts
[functions."auth-proxy"]
  timeout = 10

[functions."check-database"]
  timeout = 15

[functions."check-payment-status"]
  timeout = 15

[functions."create-checkout-session"]
  timeout = 30

[functions."delete-design"]
  timeout = 15

[functions."generate-template"]
  timeout = 60

[functions."handle-payment-success"]
  timeout = 30

[functions."list-designs"]
  timeout = 15

[functions."list-objects"]
  timeout = 15

[functions."save-design"]
  timeout = 20

# Environment variables required for production
# Set these in Netlify dashboard under Site settings > Environment variables:
# SUPABASE_URL - Your Supabase project URL
# SUPABASE_ANON_KEY - Your Supabase anonymous/public key
# SUPABASE_SERVICE_ROLE_KEY - Your Supabase service role key (for server-side operations)
# OPENAI_API_KEY - Your OpenAI API key for AI-powered template generation
# STRIPE_SECRET_KEY - Your Stripe secret key
# STRIPE_PUBLISHABLE_KEY - Your Stripe publishable key
# STRIPE_MONTHLY_PRICE_ID - Your Stripe monthly subscription price ID
# STRIPE_YEARLY_PRICE_ID - Your Stripe yearly subscription price ID
# NETLIFY_SITE_URL - Automatically set by Netlify (your site URL)

# Stripe webhook headers
[[headers]]
  for = "/.netlify/functions/stripe-webhook"
  [headers.values]
    Access-Control-Allow-Origin = "https://api.stripe.com"
    Access-Control-Allow-Methods = "POST"
    Access-Control-Allow-Headers = "stripe-signature, content-type"

# Security headers for admin pages
[[headers]]
  for = "/admin/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "camera=(), microphone=(), geolocation=()"
    Content-Security-Policy = "default-src 'self'; connect-src 'self' https://*.supabase.co https://api.stripe.com https://*.stripe.com; img-src 'self' data: https:; script-src 'self' 'unsafe-inline' https://unpkg.com https://js.stripe.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; frame-src 'self' https://*.stripe.com"